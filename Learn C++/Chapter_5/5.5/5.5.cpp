// Constant expressions

char a { 'q' };         // the value is constant but a isn't constant
const int v { 0 };      // value is constant and v is constant
const double c { 5.0 }; // value is constant but c isn't so this isn't a compile time constant